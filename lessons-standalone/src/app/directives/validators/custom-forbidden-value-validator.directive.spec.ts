import { CustomForbiddenValueValidatorDirective } from './custom-forbidden-value-validator.directive';

describe('CustomForbiddenValueValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomForbiddenValueValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
