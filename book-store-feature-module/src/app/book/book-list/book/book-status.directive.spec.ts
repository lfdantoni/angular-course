import { BookStatusDirective } from './book-status.directive';

describe('BookStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new BookStatusDirective();
    expect(directive).toBeTruthy();
  });
});
