import { BookItemStatusDirective } from './book-item-status.directive';

describe('BookItemStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new BookItemStatusDirective();
    expect(directive).toBeTruthy();
  });
});
